{
  "name": "@rigor789/typed-value-provider",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "dev": "vitest",
    "coverage": "vitest --coverage",
    "build": "vite build",
    "prepack": "yarn test && yarn build"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^3.2.4",
    "vitest": "^0.25.3",
    "@vitest/coverage-c8": "^0.25.3",
    "vite-plugin-dts": "^1.7.1"
  }
}
